## What changed

<!-- Describe the final behavior and why it matters. -->

## Validation

<!-- List commands run and manual macOS scenarios checked. Include the macOS/Xcode version for app changes. -->

## Privacy and protocol

- [ ] No raw URLs, window titles, commands, documents, browsing history, credentials, or personal host data can leave the Mac.
- [ ] Wire-format changes remain compatible with protocol v1, or introduce and document a new protocol version.
- [ ] Pause, expiry, failure, and permission-denied paths cannot leave stale public state.

## Release impact

- [ ] `VERSION` is unchanged because this PR does not publish a release.
- [ ] `VERSION` is intentionally updated and the release notes describe the user-visible change.
